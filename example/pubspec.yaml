name: flutter_liquid_glasses_example
description: Demo app for the flutter_liquid_glasses package.
publish_to: none

environment:
  sdk: ^3.13.3
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_liquid_glasses:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
